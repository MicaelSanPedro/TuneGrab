<?xml version="1.0" encoding="utf-8"?>
<!-- Loader de abertura (v0.16.0): logo, "bem-vindo" na Aston Script (fonte do
     próprio autor do app) com autoSize — o texto encolhe sozinho pra caber em
     qualquer tela/escala de fonte (mata o bug do corte "bem vin…") — e a
     assinatura "feito por micaelsan" com o nome em LedTextView -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingStart="32dp"
        android:paddingEnd="32dp">

        <ImageView
            android:id="@+id/imgLogo"
            android:layout_width="88dp"
            android:layout_height="88dp"
            android:contentDescription="@string/app_name"
            android:src="@mipmap/ic_launcher" />

        <!-- autoSize: nunca corta nem quebra linha — o Android acha o tamanho
             que cabe, em qualquer aparelho/escala de fonte do sistema -->
        <TextView
            android:id="@+id/txtWelcome"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:fontFamily="@font/aston_script"
            android:gravity="center"
            android:text="@string/splash_welcome"
            android:textColor="@color/on_background"
            app:autoSizeMaxTextSize="56sp"
            app:autoSizeMinTextSize="30sp"
            app:autoSizeStepGranularity="1sp"
            app:autoSizeTextType="uniform" />

        <LinearLayout
            android:id="@+id/madeByGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:alpha="0.85"
                android:text="@string/splash_made_by"
                android:textColor="@color/on_surface_variant"
                android:textSize="14sp" />

            <com.tunegrab.app.ui.LedTextView
                android:id="@+id/txtName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:fontFamily="@font/poppins_semibold"
                android:letterSpacing="0.12"
                android:text="@string/splash_name"
                android:textColor="@color/primary"
                android:textSize="19sp" />
        </LinearLayout>

        <ProgressBar
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_marginTop="42dp"
            android:indeterminate="true"
            android:indeterminateTint="@color/primary" />
    </LinearLayout>
</FrameLayout>
