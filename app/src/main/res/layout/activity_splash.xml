<?xml version="1.0" encoding="utf-8"?>
<!-- Loader de abertura (v0.15.0): logo, Bem-vindo em cursiva (Great Vibes)
     e a assinatura "feito por micaelsan" com o nome em LedTextView -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingStart="32dp"
        android:paddingEnd="32dp">

        <ImageView
            android:id="@+id/imgLogo"
            android:layout_width="88dp"
            android:layout_height="88dp"
            android:contentDescription="@string/app_name"
            android:src="@mipmap/ic_launcher" />

        <TextView
            android:id="@+id/txtWelcome"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:fontFamily="@font/great_vibes"
            android:text="@string/splash_welcome"
            android:textColor="@color/on_background"
            android:textSize="64sp" />

        <LinearLayout
            android:id="@+id/madeByGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:alpha="0.85"
                android:text="@string/splash_made_by"
                android:textColor="@color/on_surface_variant"
                android:textSize="14sp" />

            <com.tunegrab.app.ui.LedTextView
                android:id="@+id/txtName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:fontFamily="@font/poppins_semibold"
                android:letterSpacing="0.12"
                android:text="@string/splash_name"
                android:textColor="@color/primary"
                android:textSize="19sp" />
        </LinearLayout>

        <ProgressBar
            android:layout_width="26dp"
            android:layout_height="26dp"
            android:layout_marginTop="42dp"
            android:indeterminate="true"
            android:indeterminateTint="@color/primary" />
    </LinearLayout>
</FrameLayout>
